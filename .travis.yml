language: csharp
mono: none

cache:
  directories:
    - $HOME/.nuget/packages

#--------- TEST MATRIX --------------
matrix:
  include:
    - name: test on macos using dotnet 2.1.300
      os: osx
      dotnet: 2.1.300
    - name: test on linux using dotnet 2.1
      os: linux
      dotnet: 2.1

script:
  - ./build.sh
#-----------------------------------

#--------- DEPLOYMENT --------------
jobs: 
  include:
    - stage: deploy
      script:
        - echo "DEPLOYING PROJECT"
      os: linux
#-----------------------------------